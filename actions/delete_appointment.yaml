---
  name: "delete_appointment"
  runner_type: "run-python"
  description: "Delete an appointment from a calendar"
  enabled: true
  entry_point: "delete_appointment.py"
  parameters:
    subject:
      type: string
      description: "Used to select the profile configured in the LDAP pack's config.yaml file."
      required: true
    date:
      type: object
      description: "Date/time of appointment to delete"
      required: true
